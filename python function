# python function

함수란 중복되는것을 없애주기 위해서
★함수는 무조건 기능이 한개이다.

파이썬에서는 def 를 한다.
함수는 선언은 _함수이름

## 위치인수
함수 정의에 선언된 순서대로 인수를 차례대로 대입

## 키워드 인수
순서와 무관하게 인수를 전달하는 방법

## 가변인수
인수앞에 *을 붙여 여러개의 인수를 입력받는다.

## 람다
함수를 짭게 사용하기 위해서
특정 조건에 맞는 요소만 가져오는 함수  
-> filter  
조건에 맞는 값 찾아주기
