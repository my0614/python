'''
# vartest_global.py
a=1
def vartest(a):
    global a
    a = a + 1
vartest()
print(a)
 global 함수는 함수안에서 함수 밖의 a 변수를 직접
 사용하겠다는 뜻이다.


 '''
sum = lambda a,b: a*b
re = sum(5,6)
print(re)

''' lambda 는 함수를 생성할 때 사용하는
예약어는 def 와 동일한 역할을 한다. '''
